# Manual production deployment — 2026-09-07

Rust synchronization is running following the user's explicit authorization to rebuild managed model records as `name`/`alias`, removing previous model metadata and custom aliases. This acceptance does not assert lossless preservation of those model fields.

## Deployed scope

- Rust: `localhost/cpa-model-sync:v0.1.1`; executable SHA256 `b1661eb07e8f612635e03d70ccaff48eb4b78a7ed3afbd3ddb5f4460f119954c`.
- Companion Go CPA-member implementation: `localhost/models-enricher:v0.7.6`; executable SHA256 `44c46dd34d55dfec7c899fd0da86540772a36e476bff8c96f52232b68006b7e7`.
- Existing production metadata configuration retained, with `skip_channels: [gmicloud]` added. Expanded local metadata mappings and Axis source-only behavior were not deployed.
- CPA `v7.2.152`, Sub2API `0.2.1`, external plugins and databases were not replaced or restored.
- Runtime policy: client version `v0.65.0`; NIM exact three approved IDs, ShuaiAPI `^claude-`, GMI skipped. Other managed channels use unfiltered discovery. Defaults: 600-second interval, two channel tasks, three retries, 30-second request timeout.
- Nonroot, read-only, portless Rust container: 256MiB / 0.5 CPU; internal network only, no default route added to CPA namespace.

## Observed results

| UTC | Updated | Unchanged | Failed | Skipped | Unconfirmed |
| --- | ---: | ---: | ---: | ---: | ---: |
| 06:52:19, startup round | 9 | 1 | 1 | 1 | 0 |
| 07:02:16, natural scheduled round | 0 | 10 | 1 | 1 | 0 |

The failure is the already-disabled `glm-coding` channel (`upstream request failed`); its nine old model records remain. GMI is explicitly skipped and unchanged. Neither is reported as a successful refresh.

- Mounted Rust policy compared byte-for-byte with the installed file.
- Management readback: NIM three models; ShuaiAPI thirteen Claude models.
- YAML comparison: all non-model and unmanaged configuration unchanged. GMI and disabled GLM records unchanged.
- Second-round CPA configuration byte-identical to first-round configuration.
- First full catalog: 239 records / 11,020,392 bytes; managed-prefix membership matched the separately captured native CPA response. This was a sequential live comparison, not an atomic capture.
- Both rounds: full and authorized front returned HTTP 200; 66 visible records equaled the entitlement intersection, with record equality checked using jq. This is not a new arbitrary-precision-number regression test.
- Conditional response: HTTP 304, empty body. Anonymous and invalid-key requests: HTTP 404, empty body.
- Final 48 containers running. Except the planned Go replacement, original service identities and restart counts unchanged. Sub2API baseline restart count was three and remained three.
- Existing cgroup OOM counters did not increase across the sampled deployment; new Go/Rust counters were zero. Both APISIX containers remained limited to 128MiB. This proves the observed sequential checks, not concurrent-load or long-term acceptance, and does not erase the earlier 128MiB fixture OOM.

## Evidence and execution boundary

Authoritative private backup and evidence: production `/root/AI-gateway/.rust-manual-20260907/`. Includes fresh CPA/control backups, mounted policy, Management/native/catalog responses, container inventories, OOM counters and synchronization log. Do not commit these private payloads.

Two preceding automated attempts were rolled back because their monitor failed during planned container recreation. Evidence remains in `.rust-release-20260907/` and `.rust-release-20260907-v2/`. The third script revision was not executed. The user required manual deployment; the successful attempt used individual commands and checks, not the deployment/rollback scripts. The deployment lock was released after final checks; backups retained.

No inference requests, Axis source-only implementation, resource increases, database rollback, or automatic OpenSpec archive were part of this deployment.
