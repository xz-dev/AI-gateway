# 裸名来源链修复：两阶段已部署，等待review

2026-09-08。按用户授权顺序完成：**先保持原配置部署代码修复，确认有效；再删除已证明等价的重复绑定，第二次部署。** 未提交、推送、归档或扩展来源。测试与部署完成，不表示全部元数据已获人工验收。

## Review入口

- 表格：服务器本机 `http://127.0.0.1:9083/models-table`，两阶段均返回200；沿用用户已有访问方式，不假设公网可达。
- [当前187行CSV及逐行审查](../PLAN/enricher-bare-chain-review-2026-09-08/README.md)。保留原11列，未知、null、0和空值分别处理。
- [固定189成员的查询与字段差异](../PLAN/enricher-bare-chain-review-2026-09-08/fixed-source-review.json)。所有成员均列出查询、命中身份与完整字段变化的位置；完整原始字段值留在私有证据中。
- [汇总、在线变化名单及20项推理冲突](../PLAN/enricher-bare-chain-review-2026-09-08/summary.json)。

## 第一阶段：修代码，不改配置

`models-enricher/provider_prefix.go` 原先用空provider覆盖裸名的来源token，导致跨provider查询及错误去重。现在保留每一步明确配置的provider，只在内部转小写；复用已有查询去重、适配及合并逻辑，没有新增依赖或76条逐模型绑定。

带斜杠名称仍按首斜杠拆分并应用映射；最终token的显式ID、authType、API高于flat、模型整链替换、原生Ollama步骤、成员准入及独立token字段保持原合同。

### 修正证据

- `TestProviderPrefixOrderedQueries` 在修复前因空provider及丢失来源步骤失败；同一断言在最小修复后通过。
- 24个明确列出的顶层直接回归测试，race通过，0失败、0跳过；包含裸名Anthropic HTTP、选定作用域、多provider顺序、显式ID、共享来源失败回滚及身份边界。`go vet ./...`与格式检查通过。没有运行全项目测试或无关SSR/推理压力测试。
- 固定189成员、相同配置和四份原始来源：94条查询序列改变，78条完整来源层改变，21条最终完整记录改变。字段路径操作为新增236、覆盖65、删除43；id存在性为新增7、删除1。保存目录原本已有补全值，因此不能只看最终记录是否变化。
- bulk查询数222→415；旧78个歧义查询变为0个，修正后169次命中、227次选定作用域miss，另19个原生Ollama步骤未包含在bulk输入中。查询数增加不是逐provider HTTP请求增加。
- 原76条裸名均恢复来源输出声明；带前缀查询和完整记录在相同固定输入下不变。Nvidia API输出65536仍优先于flat的128000；MiniMax裸API ID仍正确匹配。

### 实际上线结果

原配置SHA256保持 `88b8559a24393673e047ec19313f2ef1342ec8ed6149a4311d42715e8e76d44d`，仅切换Go镜像并重建Go容器。

| 项目 | 结果 |
| --- | --- |
| 本次新鲜基线→部署后成员 | 187→187，集合和拼写不变 |
| 76条裸名输出 | 全部恢复，全部数值与固定选定来源一致 |
| 新增输入上限 | 26条，来自明确声明，不由相减推导 |
| 输出字段 | 新增76、删除1；存在60→135，缺失127→52 |
| id存在性 | 新增27、删除1；两侧都有id时没有改名 |
| 完整记录变化 | 130条；新增1363、覆盖374、删除43个字段路径操作 |

唯一失去输出字段的是 `xl/omen-alpha`：旧版越过配置作用域命中其他provider，新规则撤回这份不属于所选来源的数据。另有52条带前缀记录仅`created`改变；固定输入证明其查询未变，不把线上所有差异都归因于代码修复。

旧189项快照中的 `axis/gpt-5.2-2025-12-11`、`axis/gpt-5.4-2026-03-05` 在本次部署前的新鲜187项基线中已经缺失，不是本次部署删除。

## 第二阶段：仅简化一条已证明等价的绑定

从第一阶段上线后的生产配置派生候选，只删除：

```yaml
lookup_ids: {models.dev/meta: muse-spark-1.3-contributor}
```

XL Muse的模型级 `source_priority: [models.dev/meta]` 保留；没有把Meta加入整个XL渠道，也没有修改GMI/NIM或其他来源。

- 同一修复代码、189成员及来源输入：只有Muse查询的explicit标志变化，所选来源身份、完整来源层和189条完整结果全部相同；id存在性不变。
- 4个直接受影响的配置/来源回归测试再次以race通过，无失败或跳过。
- 第二次部署复用同一Go镜像和二进制，`.env`及有效Compose不变。配置51→50行，最终SHA256为 `f20b52abf73bf56b23e4d945b0cbed70d780bc3d6ae0f24c0fa56494a0833b8b`。
- 在线187成员不变，76条补全仍全部符合保存来源。**67条完整记录的`created`变化，其他所有字段完全相同**；完整差异保留，没有宣称在线JSON逐字节相等。

## 部署边界与制品

两阶段均手工执行Go-only重建，没有运行部署/回退编排脚本：

```sh
docker compose up -d --no-deps --no-build --pull never --force-recreate models-enricher
```

每阶段均核对实际运行二进制、挂载配置、完整运行配置/HostConfig/挂载及其他容器状态：仅Go容器身份改变，其他47个容器不变。三个 `pull_policy: never` 保留；Go仍为256MiB内存/交换总额、0.5 CPU、64 PIDs及64MiB `/tmp`，未调整安全限制。

使用既有批准用户凭据，经stdin传入，每阶段一次有界HTTP验收：健康200、完整目录200、同版本用户授权集与前端67条完整记录交集一致、ETag 304空体、匿名及无效凭据404空体、表格200。未换内部CPA身份。

| 制品 | 当前值 |
| --- | --- |
| 镜像 | `localhost/models-enricher:bare-chain-20260908T155319Z` |
| Docker镜像ID | `sha256:1dbb93d0d68868da2ba75da832ec59d98c2ec02bc007b68a278c4484ab0413a1` |
| 二进制SHA256 | `e488c0f33ef2fb135037b8a4098573c260653397234a513a1712208ad816b7f9` |
| 镜像包SHA256 | `fb0519c83eda79c77c6427f6fdf6d6a39e9131cdd12217380ec18b851d66f0ff` |

生产私有备份与完整差异：`root@<tailscale-ip>:/root/AI-gateway/.bare-chain-20260908T155319Z/`，按`stage1`/`stage2`分开。原始目录及完整运行环境只留生产私有目录，不写入公开附件。

本地证据：`/root/.cache/bare-chain-fix.rkOJzF/`，包含原源码、冻结构建源码、red/green、race命令/日志、前后真实Go重放、逐字段比较及两阶段脱敏回执。

两阶段观察窗口内Go OOM kill为0；主APISIX历史计数4未增加。不是长期稳定性结论。任务owner已核对，部署锁已释放，备份和旧镜像保留。

若用户要求回退，仅手工恢复相应阶段的目标文件并重建Go：stage2备份可恢复重复绑定而保留修复；stage1备份与旧镜像可恢复部署前状态，但会重新引入裸名错误。不能仅清空映射代替代码/配置配套回退。

## 尚未验收、尚未实施

- 当前缺项：输出上限/输出模态各52条，输入上限158条，`max_tokens`141条，`max_completion_tokens`187条；名称、上下文、输入模态、推理列表/默认值各缺1条。缺项不靠互填消除。
- **20项默认推理等级不在支持列表中**，较旧表12项增加8项。恢复高优先级来源改变了支持列表，但没有擅自猜默认值；完整名单供review。这是组合一致性问题，不是本次新增的完工门槛。
- 原60条为何没强补：其中57条缺输入上限、60条缺 `max_tokens` 和 `max_completion_tokens` 两个独立字段；按新规则复查所选来源仍无对应声明。不是认定字段没必要，而是没有获准且可引用的值；也不代表这些行所有已有值都正确。
- GMI仍是 `gmicloud: {}`，自有目录403时退CPA。**跳过自身获取的建议尚未实施**。`fetch_models: false`可以跳过403，但单独这样改不会自动补字段；如需补MiniMax-M3，还需另行确认准确来源及`MiniMaxAI`映射，继续以CPA成员为权威。
- NIM两条完整厂商路径、三条其他来源候选、准确型号miss、静态Grok父项缺失均未借本轮扩大修复。19项Ollama原生list/show原始输入未纳入固定bulk重放；在线结果通过不等于这些来源层全部采集、逐层验收。

OpenSpec实施任务按规定证据完成；完整数据正确性、渠道实测能力与人工接受仍未获确认。到此停止，等待用户review，不自动归档或继续补字段。
