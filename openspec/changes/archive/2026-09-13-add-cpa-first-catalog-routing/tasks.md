## 1. Enricher snapshot ownership (raw memberships)

- [x] 1.1 Extend the models-enricher to retain the CPA raw membership (`extractCPAIDs` capture point) and the AISIX raw membership as distinct sets per collection, never derived from the enriched union; verify with a Go test asserting an AISIX-only supplemented ID present in the enriched output is absent from the CPA raw membership
- [x] 1.2 Add an atomically published immutable routing snapshot (generation counter, per-source availability: usable/empty/failed per existing `readCache` stale-eligibility rules) owned in-process; verify with a Go test that a concurrent reader sees either the old or the new snapshot, never a mixture, and that a source failure publishes `failed` rather than empty
- [x] 1.3 Add the snapshot lifecycle: startup initialization plus bounded periodic refresh on the existing cache-TTL cadence, reusing the existing collector and singleflight, with failed refreshes retrying on the same bounded cadence (no tight loop, no new store); verify with a Go test that the snapshot initializes and refreshes with no catalog or lookup traffic
- [x] 1.4 Cover stale-eligibility and phase-isolation edges with Go tests: valid empty `{"data":[]}` overwrites last-good; auth/validation failure invalidates rather than resurrects; timeout may fall back to last-good; a failed optional AISIX collection preserves successful CPA output and known CPA decisions in the published generation
- [x] 1.5 Make catalog projection caches generation-pinned (raw generation + client_version/format key; snapshot pinned while building): stale completed projections are never served as current and late old-generation builds never overwrite the current entry, while in-flight readers may finish their pinned generation; verify with a Go test covering a generation change racing a build

## 2. Internal routing-index view (four outcomes)

- [x] 2.1 Add the bounded internal lookup endpoint (`GET /routing-index?model=<exact ID>`, bounded body/timeout) returning exactly `cpa`, `aisix`, `not_found`, or `unavailable` plus generation, where `not_found` requires both memberships authoritative and the ID absent from both; verify with a Go HTTP test (existing `handler_test.go` patterns) covering all four outcomes, authoritative absence versus failed source, and malformed/absent `model` parameters
- [x] 2.2 Verify the view never returns full catalog contents and is answered from the published generation without triggering collection (no per-lookup full build) via an existing-pattern test asserting repeated lookups between refreshes do not re-trigger collection
- [x] 2.3 Verify network restriction in compose: the endpoint is reachable only from the internal entrance path, not from any published/host binding (extend the existing boundary assertions used for the enricher listener)

## 3. Real catalog endpoints (standard + Codex split)

- [x] 3.1 Change `GET /v1/models` handling in the sidecar: absent or empty `client_version` returns the standard OpenAI `{"object":"list","data":[{"id":...,"object":"model"}]}` projection built at canonical version `1` with exact ID bytes under existing admission rules; non-empty `client_version` keeps the Codex `models[]`/slug behavior retaining the caller version; verify with a Go test covering both branches, ID byte fidelity, and that the raw CPA `NativeManifest` fetch stays pinned to `client_version=1` and models-table keeps its current inventory version
- [x] 3.2 Wire the unified internal entrance's standard models route to the Go sidecar (no-version requests no longer proxy unchanged to CPA); verify with a route-level test that both versions of `GET /v1/models` reach the sidecar and no request on that path falls through to CPA
- [x] 3.3 Add a regression test asserting the standard projection and the Codex manifest agree on ID sets under the same admission rules (no count-identity promise beyond that derivation), and that the public front standard listing still passes through Sub2API authorization/projection intersection with the parameterized behavior retained

## 4. Entrance selection in internal APISIX (bounded route seam)

- [x] 4.1 Add the AISIX upstream + internal edge to `apisix-models` configuration following existing relay/network conventions (two-member internal network, no wildcard host binding); verify with the existing compose/boundary validation scripts that no new public binding appears
- [x] 4.2 Implement per-request classification at the entrance: exactly one bounded routing-index lookup per request (no entrance-side result cache), keepalive reuse, outcome mapped to upstream selection or explicit error (`not_found` → protocol model-not-found; `unavailable`/lookup failure → 503-class with existing opacity); verify with a Lua/unit check that a failed lookup never yields not-found and exactly one upstream is chosen
- [x] 4.3 Wire the scoped high-priority selector routes for the enumerated model-bearing JSON HTTP inference paths (`POST /v1/responses`, `POST /v1/chat/completions`, including Sub2API `http_bridge` HTTP output) with precedence over legacy HTTP alias-rewrite/CPA-direct routes for those paths and sources, so the original requested ID reaches the selected backend untouched; remove conflicting legacy alias rules from inside the classified scope; verify with route-level tests that a would-be-rewritten alias ID is classified and forwarded with its original ID
- [x] 4.4 Preserve out-of-scope compatibility: legacy direct-WS route (upgrade → `ws-alias-proxy`) stays outside the classifier with no upgrade-time model inference or frame inspection; non-classified endpoints keep current matching and forwarding; verify with the existing WS-path harness and route tests that those routes are byte-for-byte unchanged in behavior
- [x] 4.5 Fail malformed classified payloads explicitly: on a classified path, invalid JSON or an unextractable model ID returns a protocol-shaped error without forwarding to any backend; verify with a Lua/unit check that no silent CPA fall-through occurs
- [x] 4.6 Preserve per-upstream service credentials (CPA header injection as today; AISIX caller-key forwarding as Sub2API already sends); verify with a check that no credential material enters logs, the index view, or error bodies

## 5. Integration acceptance (offline, fake upstreams)

- [x] 5.1 Using the existing isolated fake-upstream integration harness, verify the four routing outcomes end-to-end: CPA-only ID reaches fake CPA once; AISIX-only ID reaches fake AISIX once with no CPA attempt; overlapping ID reaches CPA only; unknown ID under authoritative snapshots returns model-not-found with no backend call; index failure or `unavailable` returns 503-class
- [x] 5.2 Verify failure semantics with fakes: chosen-backend 429/5xx/timeout surfaces to the client with no cross-backend attempt; mid-stream disconnect after commitment surfaces via the existing stream error path; Sub2API pool retry behavior and AISIX internal retry budgets remain observable within their own boundaries
- [x] 5.3 Verify the snapshot lifecycle with fakes: startup initialization and bounded-cadence refresh occur with no catalog traffic; generation advances on inventory change without listing traffic; AISIX-source outage keeps known CPA IDs routable and yields `unavailable` for would-be-AISIX decisions; CPA-source outage never guesses AISIX-only; cold start (inference-only traffic) yields `unavailable` before the first successful snapshot and recovers on the bounded cadence; a generation change racing an in-flight projection build leaves a consistent current entry
- [x] 5.4 Verify raw-source collection stays direct and non-recursive, and both catalog formats yield the same raw CPA membership baseline: with the entrance serving model routes from the sidecar, assert the enricher's raw source fetches target only the direct CPA/AISIX source endpoints (standard and `client_version=1` fakes), never the unified entrance, while client catalog delivery through the entrance continues to work

## 6. Authorization, fidelity, and parallel-change coordination

- [x] 6.1 Verify Sub2API account/group authorization still gates inference and listing (unauthorized model stays invisible in listings and unauthorized requests are rejected before backend selection) using the existing intersection/authorization harness
- [x] 6.2 Verify body/model-ID fidelity and streaming semantics end-to-end with fakes: request bodies, model IDs, SSE event order, and provider error passthrough are unchanged apart from upstream selection
- [x] 6.3 Record coordination notes for parallel changes (no edits to them): confirm `fix-sub2api-error-propagation` error surfaces flow through unchanged and `preserve-end-to-end-session-affinity` session headers are not consumed or rewritten by the seam; record the Headroom coordination gate — `add-headroom-context-compression` is separate/unimplemented/independently approved, this change does not implement or edit it, ordinary accounts target the internal entrance, and combined deployment with opted-in Headroom accounts waits for that owner's approved rebase pointing Headroom's downstream at the internal entrance; deliver as a short verification note in the change directory
- [x] 6.4 Record the Sub2API interoperability constraints for the future account/config work (apply-phase guidance, no Sub2API source/image change in scope): entrance no-cross-backend-retry is seam-scoped and leaves Sub2API pool retries (pool_mode=true, retry_count=1 kept, explicit retry_status_codes) and AISIX combo retries untouched; the `base_url` migration reads the latest whole credentials object and preserves unrelated retry/pool fields including the peer's supported unsets (peer worktree pinned v0.2.4/`5de5e2b`); rollback restores prior AISIX serving behavior (accounts currently at `http://aisix:3000/v1`) without clobbering peer edits and without leaving a CPA catch-all that cannot serve AISIX-only models; overlapping production config deployment requires prior owner diff exchange; deliver as a coordination note in the change directory

## 7. Validation and docs

- [x] 7.1 Run the full repo validation (`scripts/validate.sh` and the Go/Lua test suites) and confirm no egress-ACL, boundary, or scaffold regressions
- [x] 7.2 Confirm `openspec validate add-cpa-first-catalog-routing --strict` passes and delta specs match implementation behavior; update artifacts if verification revealed drift
- [x] 7.3 Document the follow-up (not in this change): reachability verification for the four remaining AISIX-only direct declarations, and the separately-approved production rollout procedure with backups, prior-serving-behavior rollback, and the credentials-preserving account placement migration (owned fields only, Headroom gate respected)
