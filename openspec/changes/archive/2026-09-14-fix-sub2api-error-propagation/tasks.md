## 1. Establish the actual policy and failing boundary

- [x] 1.1 Pin the executing Sub2API and Pi source/image revisions and complete the authorized sanitized policy snapshot for affected accounts; verify the confirmed explicit same-account count 1 and broad pool status list, distinguish absent custom error controls, and finish account-switch/timeout and configured temporary-unschedulable rule inventory without exposing credentials.
  - Completed by the owner-authorized sanitized production projection. The first `/proc/1/environ` attempt had stopped at a coarse permission/authentication-class boundary and did not prove SSH or PostgreSQL authentication failure; after explicit approval, a narrower `docker inspect .Config.Env` allowlist read succeeded without elevation or identity substitution. Current Sub2API is `docker.io/weishaw/sub2api:0.2.4@sha256:4a9620931fbb966b04375c34fe3edd01b640e7e6fbbba02537a9a64d9555a59e`; OCI labels pin source `https://github.com/Wei-Shaw/sub2api`, revision `5de5e2bed035d43591a2e10e51f420ef6a84eb98`, version `0.2.4`. Pi is `.155 / b0f728b8`. `/app/data/config.yaml` predates container start, no alternate config selector is set, and relevant controls resolve from pinned defaults: switches 10 (Gemini 3), `failover_on_400=false`, stream data timeout 180, keepalive 10, response-header timeout 600, OpenAI override 0, both first-output timeouts 0, passthrough timeout headers false. Accounts 1/4/5/6 are active/schedulable CPA-class API-key accounts with `pool_mode=true`, retry count 1, broad statuses `[400,401,403,404,429,500,502,503,504]`, absent custom-error and configured temporary-unschedulable controls, and no active cooldown; model mapping is absent on account 1 and has 9/4/3 entries on accounts 4/5/6. Only safe field presence/count/value classes were emitted. Evidence: `/tmp/pi-task1-1-authorized-inspect-v2/result.json` and `image-pin.json`. The projection was read-only; production mutation must still re-read each complete current object and preserve CPA `base_url` and peer-owned fields.
- [x] 1.2 Build a minimal deterministic mock reproducer in the authorized detached v0.2.4 workspace `/home/xz/Code/ai/sub2api-worktrees/fix-error-propagation`, using existing test tools and the actual forwarding path; verify at least one invocation exposes the reported generic/missing/delayed error and records the direct upstream, Sub2API, and Pi-parser results before choosing a code fix.
  - Parent-verified RED: `go test -tags unit ./internal/service -run '^TestOpenAIErrorPropagationRed$' -count=1 -v` at Sub2API `5de5e2b`, using Pi `b0f728b8`. The same final-error assertions pass for `protocol_correct_control` and fail for `native_read_failure`: the native writer joins two JSON data lines into one SSE event and omits top-level error fields, so Pi reports a JSON parse error. The transport failure occurs after a complete JSON data line but before its blank event separator, not after proven client text delivery. The direct Pi byte replay is an EOF control, not a replayed I/O exception. Verified fixture SHA-256: `8b931339f084ca89521a44ee24281bfb77ddb12cd7116d710e297c7f611002da`. This completes reproduction only; policy inventory, recovery exhaustion, and the wider finalization matrix remain unchecked.

## 2. Restore native retry scope without disabling recovery

- [x] 2.1 Prepare the supported unset/default update only for the confirmed broad same-account retry-status overrides, preserving explicit count 1 and unrelated fields; verify absent/null status lists select pinned defaults, `[]` is not used as a reset, and credential/other-policy fields remain unchanged under the real update semantics.
  - Completed offline against the pinned service update path: `AdminService.UpdateAccount` receives a latest-complete non-sensitive credentials representation with only `pool_mode_retry_status_codes` omitted; `MergePreservingSensitiveCreds` retains existing auth values while omission removes that non-sensitive key. The synthetic persisted key inventory retained `base_url`, `pool_mode`, explicit retry count 1, `model_mapping`, temporary-unschedulable fields and an unrelated custom field exactly. Missing/null selects native `[401,403,429]` behavior; explicit `[]` remains distinct and disables those retries. Parent passed 25 focused semantics tests/subtests and 16 related `UpdateAccount` tests, zero failures/skips. Evidence: `/tmp/pi-subagents-uid-1000/async-subagent-runs/c635ef3e-2780-4ec0-ae08-b6f04a231d6b/parent-semantics-verified.jsonl` and `parent-update-verified.jsonl`. This prepares the operation only: production must re-read each latest complete object through an authorized mechanism, remove only the owned key, exchange the current field diff with the routing owner, and obtain separate approval before writing.
- [x] 2.2 Verify native recovery with a retryable failure followed by success, repeated failures through exhaustion, and a failure ineligible for further native recovery; record initial attempts, same-account retries, switches and backoffs separately, proving no catch-all override or new retry layer remains in the candidate configuration.
  - Completed with candidate credentials that keep `pool_mode=true`, explicit retry count 1 and omit `pool_mode_retry_status_codes`. Default 429 then success produced calls `[73020,73020]` with one 500ms fake-time same-account backoff and no switch. Repeated 429 produced `[73010,73010,73011]`, one same-account retry, one switch, four selector calls and final 429 after later no-candidate selection. Default-ineligible 502 produced `[73020,73021]`, zero same-account retries, one immediate switch and a 0 fake-time gap before healthy fallback. Parent passed all 4 focused tests/subtests and the full handler package (1,493 pass, 0 fail, one pre-existing disabled DingTalk skip); neither candidate test contains a status override key. Evidence: `/tmp/pi-subagents-uid-1000/async-subagent-runs/81ac0238-35ad-4c7d-8c4a-9458810b28c7/parent-scope-verified.jsonl` and `parent-full-verified.jsonl`. These are explicit offline mock budgets, not resolved live global switch/timeout values.

## 3. Preserve and finalize useful errors

- [x] 3.1 Reuse native classifications, fixed safe descriptions and existing request-local failover/error objects; supply a stable non-empty generic gateway failure when no reliable classification exists. Verify recognized categories survive exhaustion and later no-candidate wrappers, while a request with no upstream attempt cannot inherit another request's error.
  - Completed locally: the default exhaustion, no-candidate selection and generic forward fallback writers reuse existing native types as non-empty codes and fixed descriptions; unknown upstream text is not copied. An actual `Responses` request with explicit retry count 1 made calls `[73010,73010,73011]`, reached a fourth no-candidate selection and retained the final 429 `rate_limit_error`. A fresh request through the same handler/repo made no new upstream call and returned its own 502 `upstream_error` code/message without the prior cause. Parent verification passed 17 focused tests/subtests and the full handler package (1,490 pass, 0 fail, one pre-existing disabled DingTalk skip); frozen SSE SHA remained unchanged. Evidence: `/tmp/pi-subagents-uid-1000/async-subagent-runs/00a9c5ed-ffed-4450-9e5e-e367454a1092/parent-focused-verified.jsonl` and `parent-full-verified.jsonl`. This does not complete WS writers, cancellation, EOF/incomplete or production policy work.
- [x] 3.2 Correct affected final HTTP, SSE, and WS/http_bridge error writers to the requested protocol; verify the real Pi parser receives non-null usable codes and safe reasons before and after response commitment, including a bare error without a later completion event.
  - Verified local slice: `sendErrorEvent` in `backend/internal/service/openai_gateway_response_handling.go` now writes a separated Responses error event with top-level `code/message`, original `sequence_number: 0`, and `param: null`. The frozen 1.2 fixture passes without changes, and Pi returns `Error Code stream_read_error: stream_read_error` with `stopReason: error`. Parent final regression `go test -json -tags unit ./internal/service -run '^TestOpenAI|^TestHandleStreamingResponse_StreamReadError' -count=1 -timeout 600s` passed 1,553 tests/subtests, with no failures or skips. Only this native SSE writer is closed; HTTP, WS/http_bridge, exhaustion and the remaining finalization cases are not claimed complete. No policy change or deployment occurred.
  - WS/http_bridge completion: bare-error EOF uses the existing synthesized `response.failed`; exhausted immediate HTTP non-2xx now also emits `response.failed` instead of a bare `type:error`, preserving a non-empty code/message and explicit `response.error.status_code`. The exact installed `responses-websocket-fetch.ts` bytes (SHA-256 `bfdfc06a04489666ea92816b7b9cb3a524552ce8abe907989da62d8af0ae333f`) were copied unchanged into the temporary pinned-Pi archive for dependency resolution. Parent composition proved one provider fetch, zero fallback fetches, one WS connection/create, no replay/completed event, and `stopReason:error` for both synthesized `invalid_request` and immediate HTTP 503 `upstream_error`. Existing query-secret masking produced `access_token=***`; ops retained intended status 503. Parent passed 2 composed and 66 bridge-family tests/subtests, zero failures/skips. The original focused reviewer withdrew its P1 prediction after tracing the existing sanitizer, approved the status fix, and explicitly approved task 3.2. Evidence: `/tmp/pi-subagents-uid-1000/async-subagent-runs/b57a6ad0-a346-4748-8613-4c0aed039618/parent-composed-verified.jsonl`, `parent-bridge-verified.jsonl`, `/tmp/pi-subagents-uid-1000/async-subagent-runs/870ea690-aa1e-4b2b-b583-8c5dfce3c63c/parent-http-pi-verified.json`, and `/home/xz/.pi/agent/sessions/--home-xz-Code-ai-AI-gateway--/subagent-artifacts/outputs/fb879f74-064c-41f3-bab6-5fd6b595c16b/ws-writer-findings-rereview.md`. This completes agreed writer/error-delivery scope, not cancellation/EOF/incomplete, arbitrary-message security, live policy or deployment.
- [x] 3.3 Complete the failed response/turn once recovery is final and cancel obsolete readers/timers; verify controlled-clock/barrier tests do not wait another backoff or keepalive interval, emit contradictory success, duplicate a terminal failure, or start new recovery after client cancellation.
  - Completion evidence from existing/current tests: retry-wait cancellation returns `FailoverCanceled`; a canceled Responses request performs only its first account call and never starts failover replay; non-stream fake time returns at the last failure; connected held-open sync/timed streams finish before keepalive/timeout; bare-error pairing permits an authoritative success or applies one authoritative failure without duplicate side effects; cancellation after output adds no synthetic error; disconnected sync/timed streams drain authoritative usage 9/2/cache 1; WS/http_bridge no-replay tests keep post-output capacity failure on the current turn. Parent independently ran the exact mapped handler/service set: 34 tests/subtests passed, zero failures/skips. The tester lane had already run the same 34 successfully but failed with WebSocket 1006 before writing its table; that infrastructure failure is not counted as acceptance. Evidence: `/tmp/pi-subagents-uid-1000/async-subagent-runs/a417cfb2-89ca-4d0b-8377-e5320e6f9b67/parent-task3-3-existing-tests.jsonl`, plus the P2 reviewer approval in `/home/xz/.pi/agent/sessions/--home-xz-Code-ai-AI-gateway--/subagent-artifacts/outputs/1dd332ee-1133-4bd5-904b-4827b0f04796/p2-drain-rereview.md`. This is local/offline lifecycle evidence, not a live timeout-policy claim.
- [x] 3.4 Preserve existing redaction and public auth/not-found opacity; verify synthetic secret-bearing, internal-address, prompt-bearing and malformed/opaque failures do not enter new client output. Native classifications use clear fixed explanations; unknown failures use a stable generic failure, and provider authentication remains distinct from client authentication.
  - Completed locally/offline. HTTP default/no-candidate tests cover credential-like values, query secrets, private IPv4/IPv6/overlay addresses, prompt-bearing and malformed/opaque bodies; final public output uses a mapped native type plus fixed text, with provider 401/403 represented as upstream authentication/access failures rather than client authentication. The new immediate WS/http_bridge `response.failed` path ignores upstream body text and maps only HTTP status to a small fixed set (401/403 upstream, 429 rate limit, 529 overloaded, other 5xx unavailable, unknown generic), while retaining the real nested status; legacy bare-error EOF keeps its existing structured-payload behavior. A composed 503 fixture proves prompt text, a private URL and token do not enter the frame, client-visible Ops message or pinned-Pi result, while one request/one frame/zero fallback and `stopReason=error` remain. Parent passed 2 composed, 70 bridge/security and 16 handler-safety tests/subtests with correct `unit` tags; counts overlap. APISIX source still globally rewrites HTTP 401/404 to an empty 404 and clears 404 response headers, and `apisix/apisix.yaml` is unmodified by this change; this is source-only, not live plugin/deployment proof. Focused reviewer approved with no P0/P1/P2. Evidence: `/tmp/pi-subagents-uid-1000/async-subagent-runs/6d67d13c-da42-4d43-8c9e-148eb246b635/parent-composed-verified.jsonl`, `parent-bridge-security-verified.jsonl`, `parent-handler-safety-verified.jsonl`, `parent-apisix-opacity-verified.json`, and `/home/xz/.pi/agent/sessions/--home-xz-Code-ai-AI-gateway--/subagent-artifacts/outputs/cb88b737-0b0d-4785-8ed9-2a3fcb0d1df6/task3-4-security-review.md`. Legacy/direct upstream terminal text and live APISIX execution remain explicitly outside this new-output slice.

## 4. Verify the composed client experience offline

- [x] 4.1 Run clean-premature-EOF and transport-exception cases separately, both before and after generated output, plus legitimate incomplete responses; verify failures are not completed as success, no post-output generation is replayed, and valid incomplete results retain their semantics.
  - Completed from existing tests, with no new fixture: pre-output clean EOF returns a failover error without client bytes; post-output clean EOF returns missing-terminal/incomplete error; transport exceptions before output trigger failover while post-output exceptions stay on the current response without replay; a valid `response.incomplete` remains successful terminal output with usage; guarded EOF still accepts a complete terminal event lacking only its final blank separator; WS/http_bridge capacity failure after semantic output remains on that turn. Scout and parent independently ran the same ten root tests; parent result was 10 pass, 0 fail/skip. Evidence: `/home/xz/.pi/agent/sessions/--home-xz-Code-ai-AI-gateway--/subagent-artifacts/outputs/9499cf6f-a5c5-4f5a-a40d-b9a2d1312970/task4-1-existing-test-audit.md` and `/tmp/pi-subagents-uid-1000/async-subagent-runs/9499cf6f-a5c5-4f5a-a40d-b9a2d1312970/parent-task4-1-verified.jsonl`.
- [x] 4.2 Run the failure matrix through the actual candidate chain with mock upstreams and the pinned Pi parser, first without Pi auto-retry and then with its existing retry-policy fixture; verify useful final errors end each attempt and distinguish new client retries from a still-open Sub2API request, recording any out-of-scope boundary that still destroys the result.
  - Completed locally/offline. A test-only chain runs a mock `HTTPUpstream` through the actual `OpenAIGatewayHandler.Responses` and fixed Pi `b0f728b8` `streamOpenAIResponses`/`retryProviderRequest`. Candidate accounts keep `pool_mode=true`, explicit retry count 1 and no retry-status override; final 502 therefore switches `[88101,88102]` without a same-account retry. With Pi `maxRetries=0`, evidence is one provider fetch, handler `[start,finish]`, two upstream account calls and one safe HTTP 502 `upstream_error`. With `maxRetries=1`, evidence is two provider fetches, strict `[start,finish,start,finish]`, account calls `[88101,88102,88101,88102]`, and two complete safe HTTP 502 bodies, proving a new client request rather than one open gateway request. Exact full account/path slices, body type/code/fixed message and final Pi error are regression-asserted; parent repeated the candidate rows ten times (30 tests/subtests), with no failures/skips. Existing candidate-native SSE/held-open rows passed 10 tests/subtests with one Pi fetch, preserved partial output and `stopReason=error`; immediate and bare-error WS/http_bridge rows passed 2 tests with one provider fetch, zero adapter fallback, one connection/create and useful terminal error. Focused reviewer approved after its sole P2 assertion-strength finding was fixed. Post-HTTP200 read/socket failure remains outside Pi provider-acquisition retry and can still destroy partial output when no terminal event is delivered; this matrix does not claim otherwise. Evidence: `/tmp/pi-subagents-uid-1000/async-subagent-runs/53256569-5ad6-4031-9efd-ea92769d194e/parent-candidate-10-verified.jsonl`, `parent-sse-matrix-verified.jsonl`, `parent-ws-matrix-verified.jsonl`, and `/home/xz/.pi/agent/sessions/--home-xz-Code-ai-AI-gateway--/subagent-artifacts/outputs/52bc6f03-f5ac-4be6-a9b9-9f540edfc665/task4-2-candidate-review.md`. Frozen SSE fixture SHA remains `8b931339f084ca89521a44ee24281bfb77ddb12cd7116d710e297c7f611002da`.

## 5. Prepare and apply only after separate approval

- [x] 5.1 Document verified default-policy semantics, error mappings, sanitized repro commands, red/green results, actual attempt counts, and finalization observations; verify claims match the fixtures and do not promise no waiting during permitted retries or all normal long generations.
  - Completed in `verification.md`. The document records immutable Sub2API/Pi/image/fixture/adapter pins; synthetic update semantics and the mandatory future latest-complete-object read; exact handler and immediate WS fixed mappings; native SSE and WS wire shapes; accepted RED/GREEN boundaries; native retry/switch and Pi new-request counters; connected finalization, cancellation and disconnected usage drain; sanitized local commands; APISIX source-only opacity; infrastructure failures excluded from acceptance; and all live/provenance/publication/deployment limits. It explicitly preserves the verified 500ms fake-time wait during permitted same-account retry, makes no healthy-generation duration claim, and records post-HTTP200 transport failure outside provider-acquisition retry. Parent checked every status/code/message against current source, qualified legacy bare-error/direct-terminal and transport-error paths, verified both command placeholders, and passed `openspec validate fix-sub2api-error-propagation --strict` plus `git diff --check`.
- [x] 5.2 Prepare the exact configuration diff and, only if code was necessary, an authorized upstream-compatible Sub2API candidate published through GitHub Actions/GHCR; verify source revision, fixed readable tag/digest, successful checks, and previous image/policy restoration references before requesting production approval.
  - Candidate published and verified by parent: upstream-compatible source branch `fix/sub2api-error-propagation` at commit `1c66619497ca7b4ea8ae677e44feb85b02c71e31` (parent baseline `5de5e2bed035d43591a2e10e51f420ef6a84eb98`), containing an exact 11-file portable candidate excluding `.pi/` and four local Pi/Bun fixtures. Source branch checks succeeded: CI run 34765327457 (https://github.com/xz-dev/sub2api/actions/runs/34765327457) and Security Scan run 34765327448 (https://github.com/xz-dev/sub2api/actions/runs/34765327448).
  - Dedicated CI branch `ci/sub2api-error-propagation-image` at workflow-only commit `ec559f48459c48bd7bca6627eb8d0109e0ade6c7` (parent `1c66619497ca7b4ea8ae677e44feb85b02c71e31`); workflow checks out exact `SOURCE_SHA` and pins action revisions without writing to default branch, `VERSION`, releases, or tags. Image build succeeded: Build run 34766089916 (https://github.com/xz-dev/sub2api/actions/runs/34766089916), plus CI run 34766089898 and Security run 34766089897.
  - Published image: readable tag `ghcr.io/xz-dev/sub2api:0.2.4-error-propagation-1c66619`; canonical immutable index reference `ghcr.io/xz-dev/sub2api@sha256:8dae4426868616f4e4cb143183aae66317d5111bc51f239d8b6a0a15c9e969b2`; one runnable `linux/amd64` descriptor `sha256:ec9f1a929eccd18fdc27835f2039d78d9fad62fd2a89c0b623e303461dc8ba05`; one unknown/unknown BuildKit provenance attestation; anonymous manifest HTTP 200; OCI labels pin source `https://github.com/xz-dev/sub2api`, revision `1c66619497ca7b4ea8ae677e44feb85b02c71e31`, and version `0.2.4-error-propagation-1c66619`.
  - Publication incident: first source push inherited `push.followTags` and unintentionally pushed 11 existing version tags (`v0.1.180..v0.1.185`, `v0.2.0..v0.2.4`); no tag-triggered Actions or Releases occurred; after explicit owner approval, all 11 fork tag refs were deleted via API and parent verified zero remain. The subsequent CI branch push used explicit `--no-follow-tags`. Recovered publication side effect, not a product or deployment failure.
  - Rollback image and policy references: current image `docker.io/weishaw/sub2api:0.2.4@sha256:4a9620931fbb966b04375c34fe3edd01b640e7e6fbbba02537a9a64d9555a59e`; accounts 1/4/5/6 old field `pool_mode_retry_status_codes: [400,401,403,404,429,500,502,503,504]` and retry count 1.
  - Latest sanitized pre-deploy read `/tmp/pi-task5-2-predeploy-read/result.json` at remote UTC `2026-09-13T15:57:03.059830+00:00` confirms old image still running and accounts 1/4/5/6 still active/schedulable CPA-class, `pool_mode=true`, retry count 1, broad field present, model_mapping counts absent/9/4/3, and no active cooldown.
  - Proposed production diff: image switches old digest -> new candidate digest; for accounts 1/4/5/6 delete only credentials key `pool_mode_retry_status_codes` (do not set `[]`); preserve explicit `pool_mode_retry_count=1`, full credentials, CPA `base_url`, model_mapping, and every peer-owned field. No production write, image switch, restart, or deployment has occurred.
- [x] 5.3 Only after approval of the exact runtime changes, apply the verified policy/image pair through the established safe procedure; verify service health, account-field preservation and permitted sanitized observations without provider failure-injection calls, leaving this task unchecked until application is actually verified.
  - Production deployment completed at remote UTC `2026-09-13T23:58:01Z`. Two earlier strict 180-second quiescence attempts had aborted before stopping the service or mutating `.env`/PostgreSQL because API key 7 (`hindsight`) continuously held request slots and occasional WS leases. The operator then explicitly authorized a short Hindsight-only maintenance interruption. The successful run required six consecutive two-second samples with zero non-Hindsight account/user/API-key/live/WS activity, observed 4 authorized Hindsight slots at the boundary, stopped only Sub2API with an explicit 15-second Docker timeout, and recorded the expected pinned 5-second handler-drain timeout; zero non-Hindsight entries raced into the sealed listener.
  - The guarded transaction locked the latest complete rows for accounts 1/4/5/6, reasserted the exact CPA/status/retry/model-mapping predicates, deleted only `credentials.pool_mode_retry_status_codes`, preserved `pool_mode_retry_count=1`, and proved every non-credential field and the credential remainder unchanged before commit. Compose recreated only Sub2API on `ghcr.io/xz-dev/sub2api@sha256:8dae4426868616f4e4cb143183aae66317d5111bc51f239d8b6a0a15c9e969b2`. Parent verification confirmed health, source/revision/version OCI labels, `/health`, exact deployment-time account hashes, unchanged current credentials, a Compose diff limited to the Sub2API image, all 51 sibling container IDs/running/health states unchanged, the old image available, and 13 protected checksum-covered rollback assets including an 81,517,055-byte PostgreSQL custom dump with 1,208 TOC lines. No provider request or failure injection was sent. Account 6 subsequently changed only runtime-owned `last_used_at`/`updated_at` as Hindsight resumed; its credentials remained byte-equivalent to the verified post-deploy snapshot. Receipt: `/root/rollouts/sub2api-task5-3-20260913T235704Z/SUCCESS.json`.
