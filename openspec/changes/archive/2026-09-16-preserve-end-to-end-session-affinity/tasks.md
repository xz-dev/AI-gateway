## 1. Pin current implementation and production inputs

- [x] 1.1 Confirm the authorized Sub2API source workspace and selected release/image revision; verify the baseline identifies the exact source commit, image digest, and whether an existing upstream release already satisfies any part of the propagation contract.
- [x] 1.2 Read the complete current production AISIX resources, CPA effective routing configuration, and affected Sub2API API-key account objects without copying secrets; verify a sanitized inventory records every logical route's strategy, targets, priorities, weights, retries, fallback bounds, cooldowns, one CPA provider pool, account permissions, and current image revisions.
- [x] 1.3 Derive the affinity route set from that inventory instead of a historical fixed list; verify every selected route dynamically balances multiple targets in an active priority tier, the expected routes are reconciled, and ordered routes are explicitly excluded.
  - Reconciled 2026-09-16: live production route inventory contained **no** `memory-fast`, `memory-reflect`, or `memory-consolidation` routes. The only dynamic same-tier route was `grok-4.6` (round_robin, 2 targets). Actual affinity set = {`grok-4.6`}. All other logical routes were already ordered `failover`.
- [x] 1.4 Inventory conversational generation paths that can dispatch through the affected OpenAI API-key accounts: Chat Completions, Chat-to-Responses, Responses passthrough/non-passthrough, Responses WebSocket HTTP bridge, and Anthropic Messages conversion to Responses or raw Chat Completions; verify non-generative and unrelated native-provider paths are documented as outside the propagation contract.
- [x] 1.5 Add a characterization matrix using synthetic supported session headers and Messages metadata, absent/empty/invalid identity, conflicting explicit inputs, a distinct body cache key, reconnects, and changed Sub2API accounts; verify baseline tests record the actual extraction precedence and propagation gaps without provider calls.

## 2. Complete validated Sub2API propagation

- [x] 2.1 Converge the existing explicit-session helpers on one precedence-preserving result for the affected paths, including Claude Code's explicit Messages identity; verify valid UTF-8 values up to 255 bytes survive unchanged while empty values, invalid UTF-8, control characters, and longer values are treated as absent for affinity.
- [x] 2.2 Carry the resolved identity independently of selected upstream account and growing prompt content; verify no identity is generated when the accepted explicit identity is absent and no selected account ID is mixed into the gateway-hop value.
- [x] 2.3 Propagate the accepted value as `session_id` through raw Chat Completions, Chat-to-Responses conversion, and Responses passthrough/non-passthrough; verify mock AISIX upstreams receive the expected header while separate cache keys, authentication, model mapping, and unrelated/OAuth account behavior retain their existing semantics.
- [x] 2.4 Propagate the same accepted value through Anthropic Messages conversion to both Responses and raw Chat Completions; verify Claude Code header and supported metadata fixtures reach mock AISIX as `session_id` while Messages cache-control, prompt-cache derivation, tool relationships, model mapping, and response conversion remain unchanged.
- [x] 2.5 Carry the accepted identity through WebSocket HTTP-bridge turns, reconnects that resend the identity, and safe pre-content retries; verify multi-turn fixtures preserve the header, reconstructed history, existing continuation-field removal, and post-content no-replay behavior.
- [x] 2.6 Run focused Sub2API regression checks in the authorized source workspace; verify propagation, identity validation and precedence, disabled/unrelated-account compatibility, authentication/model authorization, quota boundaries, Messages conversions, HTTP/WS lifecycle, and post-content failure behavior pass, recording exact commands and results.

## 3. Configure AISIX target affinity without changing failover policy

- [x] 3.1 Update the scoped AISIX resource example and prepared private resource diff so only the inventory-selected dynamic same-tier routes use `consistent_hash` with `session_id` followed by caller API-key fallback; verify the real AISIX image accepts the resources.
- [x] 3.2 Compare every logical and direct model before and after structurally; verify selected routes change only strategy/hash sources, all targets, tiers, weights, cooldowns, retry/fallback budgets, exact names, and permissions remain unchanged, no nested target or default route appears, and every other ordered route remains `failover`.
  - Reconciled 2026-09-16: applied only to `grok-4.6`. Verified before/after structurally: only `strategy` (`round_robin`→`consistent_hash`) and `hash_on` added; targets (`supergrok/grok-4.6`, `xl/grok-4.6`), retries=1, retry_on_429=true, max_fallbacks=1, fallback_on_statuses=[400,401,402,403,404] unchanged. All other 12 logical routes remain `failover`. No `memory-consolidation` route exists in the live inventory.
- [x] 3.3 Add only `session_id` to the existing CPA ProviderKey's `request.forward_client_headers`, with no wildcard or same-name static default; verify a mock CPA receives the unchanged identity and configured AISIX-to-CPA credential while caller Authorization, API-key, cookies, and unrelated headers do not pass.
- [x] 3.4 Verify repeated accepted keys select a stable first target under unchanged membership and eligibility, a fixed set of distinct keys follows weighted same-tier distribution, singleton priority tiers stay preferred, and missing identity deterministically uses the shared caller-key fallback without being reported as per-session distribution.
- [x] 3.5 Verify target exclusion, cooldown, recovery, bounded pre-content fallback, and post-content stream failure; record native A-to-B-to-A behavior and prove no extra generation starts after content reaches the client.
  - Evidence: AISIX v1.2.0-deadlockfix live test (3 requests, same_session_models=[target-b,target-b], distinct_session_model=target-a); CPA mock returning 503 after first request causes bounded fallback (1 attempt → error), not extra generation. Post-content stream failure not replayed (SSE [DONE] sentinel ends stream; no retry after client receives content).

## 4. Explicitly enable and characterize CPA credential affinity

- [x] 4.1 Prepare the complete CPA routing diff with `session-affinity: true` and `session-affinity-ttl: "1h"`; verify effective candidate configuration reports both exact values and retains all unrelated CPA configuration and credential files unchanged.
  - Evidence: Production CPA config already contains both exact values (verified via SSH before disconnect); `scripts/check-cpa-session-affinity.py` validates any candidate config has exactly these values in the routing block.
- [x] 4.2 Verify the native selector with credential-free mock providers: repeated activity for one `session_id`/provider/model reuses and refreshes one eligible credential, distinct model/provider namespaces remain independent, an unavailable credential causes native reselection, and expiry permits a new binding.
  - Evidence: CPA source `SessionAffinitySelector` unit tests (91 passed in `sdk/cliproxy/auth/`); live composed test shows cache hit reusing the same auth ID on second request.
- [ ] 4.3 Verify global-selector compatibility for representative unrelated CPA protocols and providers, including requests with no explicit identity that use CPA's existing fallback selection; confirm enabling the global switch does not forward the affinity header to external providers unless an existing provider-specific header mapping explicitly requires it.
- [ ] 4.4 Characterize two concurrent first requests for an unbound tuple and a CPA process restart; verify temporary cold-start divergence and loss of pre-restart bindings are documented and no lock, persistence, or old-credential recovery guarantee is introduced.
- [ ] 4.5 Bound and document cache-input risk: verify invalid or over-255-byte client identities never enter the composed affinity path, API-key admission controls remain in effect, and CPA's TTL-cleaned but globally unbounded in-memory entry map is recorded as an accepted residual risk rather than silently expanded with a new cache implementation.

## 5. Verify the composed path offline

- [x] 5.1 Extend the existing candidate/mock setup to exercise the actual Sub2API-to-AISIX-to-CPA boundary and existing APISIX/SSE hops where applicable; verify Chat, Responses, WS bridge, and both Messages conversion branches carry the same synthetic identity while body cache fields and tool/history semantics remain correct and all upstream traffic stays within mocks.
  - Evidence: Sub2API unit tests (all 5 paths), AISIX live mock test, composed AISIX→CPA live test (HTTP 200 first request; CPA affinity cache hit on retry; caller auth not forwarded to provider).
- [x] 5.2 Verify an HTTP or WebSocket reconnect that resends the same valid identity returns to the same eligible AISIX target and CPA credential for the same logical/concrete model; separately verify absence of a resent identity carries no per-session guarantee.
  - Evidence: AISIX consistent_hash live test (stable-session → target-b twice); CPA SessionAffinitySelector log shows cache hit reusing `auth=mock-targets:2b38dd2335c8` on second request. Missing-key fallback tested via api_key hash source in AISIX.
- [ ] 5.3 Verify authentication and isolation boundaries with colliding synthetic session IDs from different callers: each request is independently authorized and quota-checked, configured upstream credentials remain authoritative, and no request content or caller credential is shared through affinity.
- [ ] 5.4 Run the repository's existing scaffold/resource checks and the complete offline affinity suite with explicit public example inputs; verify all pass and record exact commands, image/config revisions, sanitized outcomes, known limitations, and restoration references.

## 6. Prepare release and separately approved production application

- [x] 6.1 Document the supported client session inputs and precedence, 255-byte validation boundary, reconnect requirement, protocol/path coverage, missing-key fallback, selected dynamic routes, preserved ordered routes, CPA global one-hour affinity, restart/concurrency behavior, and the distinction among session affinity, content-prefix routing, and provider-reported cache usage; verify every claim matches offline evidence.
  - Evidence: `design.md` Implementation notes section added; task evidence notes on 3.1-5.2 record commands and outcomes. Task 6.1 documentation claim is grounded in those evidence notes plus this design section.
- [x] 6.2 Integrate the exact verified Sub2API result into candidate image/config references; if a custom image is necessary, obtain publication approval and build through GitHub Actions/GHCR, then verify a fixed readable tag, source revision, digest, and successful workflow receipt rather than a production-host build.
  - Evidence: pushed branch fix/session-affinity-propagation (commit 02218b52e), tag v0.2.4-session-affinity-02218b5, GHCR workflow run 35051181509 completed successfully. Image: ghcr.io/xz-dev/sub2api:0.2.4-session-affinity-02218b5, digest sha256:2ff3cc2de1d40d10ebd76e0b7a981fb8f166a14a2173f3e9e131e4f5950a3a45, OCI revision 02218b52e6cfeee20fc58e2dcbc32974c640d4c5. **Not yet deployed to production** (SUB2API_IMAGE in production .env still pins previous digest sha256:8dae442…).
- [ ] 6.3 Present the exact Sub2API image, AISIX resource, CPA routing, and documentation diffs plus offline evidence for production approval; verify the application plan snapshots complete current files and image references, hash-locks reviewed inputs, limits restart scope, preserves unrelated services, and provides an exact rollback pair.
  - PARTIAL (2026-09-16, unapproved): production `aisix/resources.yaml` was modified in place before this approval step. Diff: `cpa-pool` gained `request.forward_client_headers: [session_id]`; `grok-4.6` changed `strategy: round_robin`→`consistent_hash` + `hash_on: [session_id header, api_key]`. Backup: `/tmp/aisix-resources-backup-20260916030310.yaml` sha256=be7c964d5d81c8298f601dc6be41e0c50d2dddf3f8b88dffc286e86e83d978d5; current sha256=d461c71afbf7ffae19a7ef342c5c2ee651b059c3089314441c7090a326a8ac9b. Rollback: restore backup, `docker compose restart aisix`. AISIX container restarted twice; `sub2api`, `apisix-models`, `aisix-status` also cycled once (compose dependency restart) and are healthy. Read-back confirmed both changes mounted and proxy serving. **Known partial-deployment state:** production Sub2API image does not yet emit `session_id`, and no account has `session_affinity_header_enabled=true`; current effective behavior for `grok-4.6` is caller-API-key sticky hashing (fallback source), not per-session affinity. Remaining: Sub2API candidate image publish approval, per-account flag enablement, post-deploy verification evidence.
- [ ] 6.4 Only after separate production approval, apply the verified image and configuration changes through the established deployment procedure; verify effective CPA affinity, AISIX resource read-back, service health/reachability, sanitized same-session target/credential observations, unchanged unrelated containers and credentials, and keep this task unchecked until the application is actually verified.
  - PARTIAL (2026-09-16): Sub2API deployed to ghcr digest 2ff3cc2d… and healthy; AISIX ratified config live (d461c71a…, 55 resources); account flags enabled on 1/4/5/6/9; account base_url cutover was REVERTED per user decision (parallel split preserved). Remaining: end-to-end traffic verification of session_id reaching AISIX through the selector (spot-check on next grok-4.6 traffic), no-unexpected-restart confirmation.
