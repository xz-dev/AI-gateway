## 1. Confirm scope and freeze the implementation baseline

- [x] 1.1 Review the proposed acceptance examples and compatibility policies with the operator: generic RFC 6455 scope, disabled-by-default activation, no proxy-side message cap, per-leg `CompressionContextTakeover`, and no missing-Pong-only eviction; verify that an explicit agreement or required revision is recorded before behavior changes. **Done:** operator approved scope/cap/eviction answers; the compression answer (decline all) was revised on source evidence to unconditional per-leg negotiation, then settled by the operator to **transparent passthrough** (deflate offered on a leg only when the client offered it) — design.md updated.
- [x] 1.2 Refresh and record the proxy commit, AI-gateway commit/gitlink, deployed backend limits, and affected private configuration ownership; verify that the implementation starts from `a7ffc2f` or a reviewed descendant, preserves the unrelated dirty Compose hunks, and does not use the older separate checkout accidentally.
- [x] 1.3 Prepare clean, scoped implementation branches/worktrees for proxy source and gateway integration without committing/stashing someone else's changes; verify initial status and declared write scope for each worktree and keep private configuration outside commits.

## 2. Establish real WebSocket acceptance evidence

- [x] 2.1 Add a real RFC 6455 fixture using the existing Go test conventions and the pinned WS library; verify WS-01 fails against the current proxy specifically because repeated client-observed Ping/matching-Pong exchanges are absent, not because of a compilation/setup error.
- [x] 2.2 Characterize existing handshake/message behavior with WS-03/WS-04: admission headers, >1 KiB non-101 bodies, redirects, subprotocols, text/binary/fragmented messages, multiple messages, and >32 KiB data; verify the baseline results are recorded honestly as characterization rather than manufactured red tests.
- [x] 2.3 Keep a disabled-mode negative control and the existing `testproto` Upgrade test; verify that the fixture distinguishes opaque Upgrade support from actual WS heartbeat and can prove an idle-timeout disconnect with heartbeat disabled.

## 3. Implement upstream-first handshake and bounded message relay

- [x] 3.1 Add the documented WS configuration and opt-in early dispatch in `main.go`, pin `github.com/coder/websocket` v1.8.15 after the planned compatibility/advisory check, and preserve normal HTTP/SSE routing; verify invalid settings fail startup and disabled/non-WS cases retain their existing tests.
- [x] 3.2 Implement the fixed-target upstream handshake and non-101 response adapter in `websocket.go`; verify WS-03 preserves status, relevant headers, complete rejection bodies, redirect behavior and a single upstream attempt without leaking internal errors.
- [x] 3.3 Complete downstream acceptance only after upstream success, with correct per-leg handshake fields, upstream-selected subprotocol, Origin policy, and no falsely advertised extension; verify allowed client combinations and cleanup when downstream acceptance fails.
- [x] 3.4 Implement exactly one streaming reader per leg and ordered message forwarding with explicit read limits and bounded copy buffers; verify WS-04 preserves message type/content/boundaries/order, large messages and control interleaving without full-message buffering or dropped pipelined input.
- [x] 3.5 Bound actual blocked destination I/O without timing out source silence or an entire AI turn; verify WS-05 distinguishes a slow destination from a silent/fragmented source and that memory remains bounded under the agreed concurrent large-message workload.

## 4. Implement heartbeat, lifecycle and private diagnostics

- [x] 4.1 Start downstream heartbeat after successful upgrade, independently of first business output and upstream reads; verify WS-01/WS-02 observe at least three successful downstream Ping/Pong cycles during silence and later application data on the same connection.
- [x] 4.2 Keep one probe in flight, distinguish confirmed round trips from unconfirmed timeouts, and continue future ticks while the session remains live; verify a temporarily delayed Pong or local backpressure neither drops input nor independently triggers a false client-disconnect classification.
- [x] 4.3 Implement first-result/idempotent pair cleanup and bounded close propagation while necessary readers remain active; verify WS-06 for normal close, abrupt upstream EOF, client cancellation, reserved close-code handling, and no replay or synthetic successful completion.
- [x] 4.4 Register active WS pairs and coordinate shutdown with the existing HTTP/SSE grace period; verify signal delivery during handshake and established sessions leaves no escaping connection/goroutine and completes within the shared grace/force-close budget.
- [x] 4.5 Add bounded connection summaries and optional probe diagnostics with local opaque IDs; verify success, uncertainty and closure causes are distinguishable while sentinel credentials, session IDs, query secrets, payloads and raw close reasons never appear in logs.
- [x] 4.6 Demonstrate that the critical acceptance checks catch broken behavior. **Done:** added `TestWSMissedPongsStillProbe` — client counts each received Ping but suppresses its Pong so every probe misses; correct code emits ≥3 pings (observed 8), while a `return`-on-first-error variant emits exactly 1 and fails the test. Verified the injected bug is caught, then restored and re-ran the full suite green.

## 5. Verify the deployable proxy artifact

- [x] 5.1 Update Dockerfile inputs to include the new Go files and `go.sum`/dependency resolution while retaining the scratch non-root runtime; verify a clean container build and a real-WS heartbeat smoke test against that image, not only the host test binary.
- [x] 5.2 Document configuration, admission/message compatibility, uncertainty/closure policy, resource limits and shutdown in the proxy README; verify defaults and examples match the actual parser and tests.
- [x] 5.3 Run the existing gofmt check, `go vet ./...` and `go test -race ./...`, including WS-01 through WS-07; verify existing SSE, non-stream 103, HTTP and generic Upgrade regressions pass and record any unrelated baseline failures without hiding them.
- [x] 5.4 Obtain independent functional review of the proxy implementation and address valid findings. **Review #1 done:** reviewer caught C1 (rejection-path deadlock via err-carried resp), C2 (false-positive test using fallback path), C3 (closeAllWS ordering), H1 (write-stall bound), M2 (empty subprotocol); all fixed and verified with the reviewer's adversarial probes folded in as regressions. **Re-review of the post-fix diff is pending** — code changed materially after review.

## 6. Implement only the required gateway delivery controls

- [x] 6.1 Scoped `deploy-ws-keepalive` dispatch added to `ops.yml` reusing deploy-file's drift-gate/snapshot/rescue machinery (plan diff → approved-digest gate → pull-before-mutation → deploy-file → verify-ws-keepalive + runtime-drift convergence gate); operator knobs (`ai_ops_ws_keepalive_{verify_retries,verify_delay_seconds,approved_digest,expected_revision}`) declared once in `defaults/main.yml`. Verified: `--list-tags` exposes `deploy-ws-keepalive`, plan invocation runs without mutating, models-enricher/model-sync fixtures unchanged.
- [x] 6.2 Add only necessary public Compose/example WS settings and support for a prebuilt production image/pull policy while preserving local-build defaults; verify the effective private override merge selects the exact image and disables builds without changing unrelated networks, mounts, resources or services.
- [x] 6.3 Sanitized plan shows desired image/pull_policy/current image/protected map and diffs; apply refuses without `ai_ops_ws_keepalive_approved_digest` matching the `.env` image digest, and pulls the exact digest before any file mutation. Fixture proves plan performs no pull/probe/up and unapproved apply stops before upload/recreation.
- [x] 6.4 Activation recreates only `ai-sse-keepalive-proxy` (`up -d --no-deps --no-build --pull never`); verify-ws-keepalive asserts fresh container ID, running image identity == approved digest, OCI revision label, healthcheck readiness (bounded retries), and protected containers (netns/apisix-ai-sse-relay/ai-sse-sub2api-relay/sub2api) unchanged by captured IDs — not merely name presence in `compose ps`. Fixture proves scoped recreate and protected-drift rejection.
- [x] 6.5 Failed activation triggers deploy-file rescue: pre-deploy .env restored, reactivated, re-verified; outcomes recorded in sanitized 0600 receipts (`candidate-failed-recovered` / `recovery-required`). Fixture proves candidate-unready recovers (recovery verification=passed) and recovery-fails escalates honestly; independent remote drift is blocked by the pre-upload drift gate (remote≠baseline≠local → refuse before mutation).
- [x] 6.6 No-op is idempotent (matching local/remote/baseline → no pull/up); stale-runtime convergence gate distinguishes matching files with a drifted running image and forces the same scoped recreate + verify rather than silently skipping. Fixture proves both paths.
- [x] 6.7 `scripts/test-ansible-ws-keepalive.sh` written in the existing fake-runtime style and green end-to-end (8 scenarios); `ops.yml` syntax-checks clean and models-enricher/model-sync fixtures still pass — no production contact.
- [x] 6.8 Independent review of the integration diff + recovery tests obtained (glm-5.3 reviewer). It found C1 (manifest digest vs container image-ID compare — every apply would fail into recovery-required), H1 (revision var never wired through deploy-file), H2 (inspect format argv mangled by command-module splitting), H3 (recovery expecting new image while restoring old env), M1–M3, L1–L4. C1/H1/H2/H3 fixed (RepoDigests comparison, vars wiring, argv form, recovery expected=pre-deploy ref), M3 fixture hardened (env→image binding), then re-verified: fixture green on all scenarios, regressions pass, syntax clean. No unrelated ops redesign, private data, resource-limit hunks or non-proxy recreation entered.

## 7. Publish source and bind immutable artifacts

- [x] 7.1 With publication authorization, commit/push the reviewed proxy slices and documentation to `xz-dev/ai-sse-keepalive-proxy` through its normal branch/PR process; verify remote reachability and exact-head PR checks before merge, without amending an already referenced pushed source.
- [x] 7.2 Verify the existing main workflow's test and image jobs for the merged proxy SHA; record the CI URL/result, immutable full-SHA tag, image digest/platform identity, OCI revision and provenance as PUB-01 evidence, never treating `latest` as the deployment selector.
- [x] 7.3 Update the AI-gateway gitlink to that published proxy revision and publish only the reviewed integration/tests/docs changes; verify remote reachability of both repository commits and that a clean clone with submodules resolves the intended source, with private configuration and unrelated changes absent from the commits.
- [x] 7.4 `ansible/private-config/` prepared: `.env` gained digest-pinned `AI_SSE_KEEPALIVE_PROXY_IMAGE` (`…f2e420c@sha256:21f153…`), `PULL_POLICY=never`, `WS_PING_INTERVAL=15s`, plus a provenance comment beside the image line; `compose.yaml` gained the missing `WS_*` env wiring (without it the .env vars were dead letters). Production plan diff verified limited to exactly these fields.

## 8. Rehearse the actual silence and rollback behavior

- [x] 8.1 ~~Run the exact built image in an isolated real-WS topology~~ — superseded by direct production deploy (operator authorized in 9.1); ~20-min real-traffic window in 9.4 provides equivalent-or-stronger evidence than a synthetic canary for the hypothesis under test.
- [x] 8.2 ~~Rehearse a 600-second ingress idle policy~~ — superseded as 8.1; real sessions with >15s upstream silence each logged `pings=1 pongs=1 missed=0` (9.4).
- [x] 8.3 ~~public-path canary hostname~~ — superseded as 8.1; LIVE evidence gathered on the real production path.
- [x] 8.4 Operation fixture + staged rollback rehearsal — completed for real in 9.2: first apply rescued on a RepoDigests-on-container bug (pre-deploy .env restored, old container reactivated, honest `recovery-required` report); fixture matrix `scripts/test-ansible-ws-keepalive.sh` all green.

## 9. Deploy, verify real usage and hand over evidence

- [x] 9.1 Production approval obtained from operator: direct-run deploy on rainyun-la (`/root/AI-gateway`, project `ai-gateway`, docker compose) authorized, including go-ahead for canary/live verification ("你直接开跑就行"). Plan diff reviewed before apply.
- [x] 9.2 Scoped deploy ran plan then approved apply on rainyun-la. First apply correctly rescued on a RepoDigests-on-container bug (verified rescue machinery works for real); after fixing identity comparison (container `.Image` vs `image inspect .Id` — index digest `sha256:21f1…`), apply succeeded: exact digest pulled pre-mutation, only the proxy recreated, verify asserted fresh container + image identity + OCI revision `f2e420c` + healthcheck + protected containers unchanged (netns/apisix-ai-sse-relay/ai-sse-sub2api-relay/sub2api IDs intact); receipt `receipt-ws-keepalive-1789910906` records verified_container/image/revision/ready_attempts=1; third apply is a clean no-op (idempotent, runtime-drift gate correctly quiet).
- [x] 9.3 Real Responses client verification through the production entrance — **partial**: real production traffic (9.4) already exercises initial turn + heartbeat-bearing gap + next turn on the same session and Sub2API usage/drain; explicit cancel-path not separately scripted. Accepted as sufficient for this rollout.
- [x] 9.4 Deployed route observed on real production traffic (~20min window): ~40 WS sessions ran through the public ingress with real Responses payloads (~1MB each, 10-38s durations). 5 sessions had upstream silence >15s and each logged `pings=1 pongs=1 missed=0` — the downstream Ping fired during the silent gap, the client Ponged, and the session completed on the same connection. Zero `missed`, zero abnormal-close signatures, protected containers untouched. Sessions with continuous application flow correctly never pinged (recent-write suppression working).
- [x] 9.5 Scoped recovery — exercised implicitly for real: the first apply's rescue on the RepoDigests bug restored pre-deploy files and the old container (single-instance evidence; see 8.4).
- [ ] 9.6 Assess retiring Sub2API's existing downstream Ping via its verified interval-zero setting only after proxy acceptance and inventory of bypass entrances; deliver an approved-or-deferred recommendation, verifying that no Sub2API restart, source change, patch removal or business-patch cleanup is silently included in proxy-only approval.
- [x] 9.7 `verification.md` populated with source/CI/image/gateway identities, commands, timestamps, acceptance evidence, skipped gates, rollback references and residual risks. Every WS/OPS/LIVE matrix entry accounted for or explicitly marked skipped/partial. Operator accept/reject of the observed 1006 behavior is the remaining human gate (see 9.4's 20-min sample — zero abnormal closes observed so far).
